{
  "name": "redux-api-call",
  "version": "0.0.1",
  "description": "Redux utilities for making API calls",
  "scripts": {
    "pretest": "rollup -c rollup.test.config.js -m",
    "test": "mocha --require source-map-support/register --require isomorphic-fetch .build/tests.js",
    "test:watch": "npm test || true && chokidar '{src}/**/*.js' -c 'npm test'"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "isomorphic-fetch": "^2.2.1",
    "lodash": "^4.16.1",
    "redux": "^3.6.0",
    "redux-mock-store": "^1.2.0",
    "rollup": "^0.36.0",
    "rollup-plugin-alias": "^1.2.0",
    "rollup-plugin-buble": "^0.14.0",
    "rollup-plugin-multi-entry": "^2.0.1",
    "rollup-plugin-strip": "^1.1.1",
    "rollup-plugin-stub": "^1.1.0",
    "rollup-plugin-uglify": "^1.0.1",
    "sinon": "^1.17.6",
    "source-map-support": "^0.4.2",
    "timekeeper": "^0.1.1"
  }
}
